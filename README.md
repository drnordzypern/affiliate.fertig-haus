# Fertig Haus Partner Portal — Frontend

The public, Partner-facing frontend for the future Fertig Haus Partner
Portal, intended for `https://affiliate.fertig-haus.net`. This repository
is the frontend/BFF side of the architecture; `saleschain-os` remains the
backend source of truth for Partner data, referrals, commissions,
authentication, and compliance.

## Current preview scope

This is a **frontend foundation**, not a full production system, with one
real, functional slice: invitation acceptance and Partner-session
login/logout, mediated entirely through this app's own same-origin BFF.
Concretely:

- All pages render real, professional German copy and a finished visual
  design — nothing here is a wireframe.
- The invitation-acceptance flow (`/einladung#token=...`) and its
  two BFF routes (`app/api/partner-invitations/accept`,
  `app/api/partner-sessions/logout`) are real and call SalesChain's
  approved public invitation/Partner-session endpoints server-to-server.
- The Partner application form and the Partner portal's business data are
  still **non-functional previews**, clearly labelled as such in the UI.
  No real Partner, lead, referral, or commission data exists or is
  simulated anywhere in the app. The portal does not gate itself on the
  session cookie — see "Security boundaries" below for why.
- The site is configured to not be indexed by search engines
  (`app/robots.ts` plus `robots` metadata) until a real portal is
  approved and launched.

See [`docs/architecture.md`](docs/architecture.md) for the full
architecture boundary and the security rules this integration follows.

## Architecture

```text
Browser
→ affiliate.fertig-haus same-origin BFF
→ SalesChain public/API boundary
→ SalesChain database and business logic  (source of truth)
```

This repository owns the browser-facing UI and the same-origin BFF routes
that mediate calls to SalesChain. It does not and will not own Partner
business logic, persistence, or SalesChain tokens on the client.

## Tech stack

- [Next.js 16](https://nextjs.org/) — App Router, TypeScript (strict
  mode), Turbopack (default dev/build)
- [React 19](https://react.dev/)
- [Tailwind CSS 4](https://tailwindcss.com/) — CSS-first theme
  configuration in [`app/globals.css`](app/globals.css)
- [ESLint 9](https://eslint.org/) — flat config, `eslint-config-next`
- [Vitest](https://vitest.dev/) + [React Testing
  Library](https://testing-library.com/docs/react-testing-library/intro/)
  — component/unit tests, `happy-dom` environment

## Local setup

Requires Node.js **24+** (a currently maintained Node release — Node 20
reached end-of-maintenance and is not supported) and npm.

```bash
npm install
npm run dev
```

The app runs at `http://localhost:3000`.

## Commands

| Command | Description |
| --- | --- |
| `npm run dev` | Start the Next.js dev server (Turbopack) |
| `npm run build` | Production build |
| `npm run start` | Serve the production build |
| `npm run lint` | ESLint |
| `npm run test` | Run the Vitest suite once |
| `npx tsc --noEmit` | Type-check without emitting output |

## Routes

| Route | Purpose | Status |
| --- | --- | --- |
| `/` | Landing page: positioning, process overview, benefits, audience, trust & security, CTA | Live content |
| `/so-funktioniert-es` | Full Partner lifecycle explanation (application → commission release) | Live content, describes future functionality |
| `/partner-werden` | Partner application form preview | Preview — submission disabled |
| `/einladung#token=<invitation-token>` | Canonical invitation-acceptance flow | Real — fragment token, immediate scrubbing, Turnstile, BFF submission |
| `/partner/invitation/accept#token=<invitation-token>` | Compatibility alias for `/einladung` | Real — same shared implementation; new links must not use it |
| `/portal` | Partner portal information architecture preview, plus logout | Preview data; real logout control |
| `/robots.txt` | Generated by `app/robots.ts`; disallows all crawling | Active |

## Environment variables

See [`.env.example`](.env.example) for the full list with explanatory
comments. **Names only — no values are set, and none should be
committed.**

- `SALESCHAIN_API_BASE_URL` — server-only, SalesChain boundary base URL
- `NEXT_PUBLIC_TURNSTILE_SITE_KEY` — intentionally public (Turnstile site
  key); SalesChain verifies the token server-side

## Security boundaries

- SalesChain is the sole source of truth for Partner applications,
  invitations, referrals, leads, commissions, verification, and
  audit/compliance. This repository implements only the narrow,
  already-approved invitation-acceptance and Partner-session boundary —
  see [`docs/architecture.md`](docs/architecture.md).
- No database, cache, or persistence layer belongs in this repository.
  The one exception is the browser's own HttpOnly Partner-session cookie.
- The browser only ever talks to this app's own origin. The same-origin
  BFF (`app/api/partner-invitations/accept`,
  `app/api/partner-sessions/logout`) is the only thing allowed to call
  SalesChain.
- Server-only configuration (see `.env.example`) is never exposed to the
  browser, logged, or embedded in a `NEXT_PUBLIC_*` variable.
- Invitation tokens travel only in URL fragments, never query strings,
  and are never logged, sent to analytics, or persisted in browser
  storage. The bootstrap token and the Partner session token never reach
  browser JavaScript at all.
- **Cookie presence is not proof of a live SalesChain session** —
  SalesChain has no `whoami` endpoint yet. See "Known limitation" in
  [`docs/architecture.md`](docs/architecture.md).

Full detail: [`docs/architecture.md`](docs/architecture.md).

## What is intentionally not implemented

- A working Partner application submission
- Referral/QR attribution, lead tracking, or commission calculation
- Any authoritative, live validation of the Partner-session cookie
  (blocked on a future SalesChain `whoami` endpoint)
- Analytics, tracking pixels, or a cookie banner (none are needed — no
  tracking exists yet)
- A sitemap (would claim indexable, finished routes; none exist yet)
- Final, legally reviewed Impressum/Datenschutz copy or links

## Next integration steps

1. Wire `/partner-werden` to a real, Turnstile-protected BFF submission
   endpoint once the application data contract is approved.
2. Get a SalesChain `whoami` / Partner-resource contract reviewed and
   approved, then replace the `/portal` preview with an authenticated
   view backed by real SalesChain data.
3. Replace the temporary text wordmark
   ([`components/brand/Wordmark.tsx`](components/brand/Wordmark.tsx)) with
   the approved logo, and add an approved favicon (`app/icon.png` or
   `app/favicon.ico`) — none is set today, so browsers fall back to their
   own default tab icon rather than showing unrelated Next.js branding.
4. Supply final, legally reviewed Impressum/Datenschutz links in
   [`lib/legal-config.ts`](lib/legal-config.ts).
5. Re-enable indexing (`app/robots.ts`, `robots` metadata in
   `app/layout.tsx`) and add a sitemap once the portal is approved for
   launch.

This project is a frontend foundation and is **not** production ready —
only the invitation-acceptance/Partner-session slice described above is
real.
